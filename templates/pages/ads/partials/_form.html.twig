{{ form_start(form, { 'attr': {'class': 'bg-secondary bg-opacity-75 rounded-4 text-tertiary p-5'} }) }}

    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.firstname) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.lastname) }}
        </div>
    </div>

    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.customerEmail) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.phoneNumber) }}
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            {{ form_row(form.subject) }}
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            {{ form_row(form.comment) }}
        </div>
    </div>

    <button class="btn btn-success"><i class="bi bi-envelope-arrow-up"></i> {{ button_label|default('Envoyer') }}</button>

{{ form_end(form) }}