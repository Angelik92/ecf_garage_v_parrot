    <section class="ads pt-5">
        <div class="container d-flex flex-column justify-content-center align-items-center text-tertiary">
            <h2 class="fw-bold mt-5"> Les dernières annonces</h2>
            <p class="mt-3 w-75">Ne manquez pas nos dernières offres exceptionnelles sur les véhicules d'occasion ! Venez dès maintenant dans notre garage et profitez des meilleures opportunités pour trouver votre prochaine voiture.</p>

            <article class="row row-cols-1 row-cols-md-3 g-4 mt-4">

                {% for ad in ads %}

                    <div class="col mb-5">
                        <div class="card h-100 w-100">
                            {% if ad.pictures is not empty %}
                                {% set first_picture = ad.pictures[0] %}
                                <img class="ad__card__image w-100 h-35" src="/uploads/{{ first_picture.path }}">
                            {% else %}
                                <img src="{{ asset('build/images/vehicule-defaut.jpg') }}" class="ad__card__image w-100 h-35" alt="photo du véhicule">
                            {% endif %}
        
                            {# Displaying ad details such as title, vehicle information, and price #}
                            <div class="card-body d-flex flex-column justify-content-between">
                                <div>
                                    <h3 class="card-title">{{ ad.title | upper}}</h3>
        
                                    <p><span class="fw-bold"> {{ ad.car.brand.name | upper}} </span> - {{ ad.car.model | capitalize}}</p>
                                    <p>{{ ad.car.gearbox.label | capitalize}} | {{ ad.car.fuel.label | capitalize }} | {{ ad.car.power }} CH
                                    <p class="card-text">{{ ad.description | capitalize}}</p>
                                </div>
        
                                <div class="d-flex flex-column justify-content-center">
                                    <p class="fw-bold fs-3 text-center"> {{ ad.price }} €</p>
                                    <a href="{{ path('ads_info', {'id': ad.id}) }}" class="btn btn-primary">Plus d'informations</a>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </article>
        
        <a href="{{ path('ads') }}" class="btn btn-outline-tertiary my-5"> Voir toutes les annonces</a>
        </div>

    </section>