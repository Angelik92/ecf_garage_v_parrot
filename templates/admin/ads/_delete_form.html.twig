<form method="post" action="{{ path('ads_delete', {'id': ad.id}) }}" onsubmit="return confirm('Souhaitez-vous supprimer {{ ad.title | upper }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ad.id) }}">
    <button class="btn btn-danger"><i class="bi bi-trash3"></i> Supprimer</button>
</form>
