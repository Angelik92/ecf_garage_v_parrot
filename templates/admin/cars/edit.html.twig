{% extends 'base_db.html.twig' %}

{% block title %}Modifier le modèle{% endblock %}

{% block body %}
    <div class="container-fluid d-flex flex-column justify-content-evenly align-items-center text-primary">
        <h1 class="text-light fw-bold">Modifier le modèle {{ car.model | upper }}</h1>

        {{ include('admin/cars/_form.html.twig', {'button_label': 'Mise à jour'}) }}

        <a class="btn btn-primary" href="{{ path('cars_index') }}"><i class="bi bi-arrow-return-left"></i> Retour à la liste</a>

        {{ include('admin/cars/_delete_form.html.twig') }}
    </div>
{% endblock %}
