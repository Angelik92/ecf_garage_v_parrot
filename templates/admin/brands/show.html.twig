{% extends 'base_db.html.twig' %}

{% block title %}Brands{% endblock %}

{% block body %}
    <div class="container-fluid d-flex flex-column justify-content-evenly align-items-center text-primary">
        <h1 class="text-light fw-bold">Voir {{ brand.name | upper }}</h1>

        <table class="table table-primary">
            <tbody>
                <tr>
                    <th>Nom de la marque</th>
                    <td>{{ brand.name | upper}}</td>
                </tr>
                <tr>
                    <th>{{ include('admin/brands/_delete_form.html.twig') }}</th>
                    <td><a class="btn btn-warning"  href="{{ path('brands_edit', {'id': brand.id}) }}"><i class="bi bi-pencil-square"></i> Modifier</a></td>
                </tr>
            </tbody>
        </table>

        <a class="btn btn-primary" href="{{ path('brands_index') }}"><i class="bi bi-arrow-return-left"></i> Retour à la liste</a>

    </div>


{% endblock %}
