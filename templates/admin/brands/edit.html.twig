{% extends 'base_db.html.twig' %}

{% block title %}Modifier Marque{% endblock %}

{% block body %}
    <div class="container-fluid d-flex flex-column justify-content-evenly align-items-center text-primary">
        <h1 class="text-light fw-bold">Modifier la marque {{ brand.name | upper }}</h1>

        {{ include('admin/brands/_form.html.twig', {'button_label': 'Mise à jour'}) }}

        <a class="btn btn-primary" href="{{ path('brands_index') }}"><i class="bi bi-arrow-return-left"></i> Retour à la liste</a>

        {{ include('admin/brands/_delete_form.html.twig') }}
    </div>
{% endblock %}
