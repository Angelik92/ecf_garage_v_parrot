{% extends 'base_db.html.twig' %}

{% block title %}Modifier le service{% endblock %}

{% block body %}
    <div class="container-fluid d-flex flex-column align-items-center justify-content-evenly text-primary">
        <h1>Modifier le service : </h1>

        {{ include('admin/services/_form.html.twig', {'button_label': 'Mise à jour'}) }}

        <a href="{{ path('services_index') }}"><i class="bi bi-arrow-return-left"></i> Retour à la liste</a>

        {{ include('admin/services/_delete_form.html.twig') }}
    </div>
{% endblock %}
