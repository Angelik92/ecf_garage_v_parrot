{% extends 'base_db.html.twig' %}

{% block title %}Nouveau service{% endblock %}

{% block body %}
    <div class="container-fluid d-flex flex-column align-items-center justify-content-evenly text-primary">
        <h1 class="text-light fw-bold">Nouveau service</h1>

        {{ include('admin/services/_form.html.twig') }}

        <a class="btn btn-primary" href="{{ path('services_index') }}"><i class="bi bi-arrow-return-left"></i> Retour à la liste</a>
    </div>
{% endblock %}
