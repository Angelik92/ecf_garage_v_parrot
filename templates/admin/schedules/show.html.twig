{% extends 'base.html.twig' %}

{% block title %}Schedules{% endblock %}

{% block body %}
    <h1>Schedules</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ schedule.id }}</td>
            </tr>
            <tr>
                <th>Morning_schedule</th>
                <td>{{ schedule.morningSchedule }}</td>
            </tr>
            <tr>
                <th>Afternoon_schedule</th>
                <td>{{ schedule.afternoonSchedule }}</td>
            </tr>
            <tr>
                <th>Day</th>
                <td>{{ schedule.day }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('schedules_index') }}">back to list</a>

    <a href="{{ path('schedules_edit', {'id': schedule.id}) }}">edit</a>

    {{ include('admin/schedules/_delete_form.html.twig') }}
{% endblock %}
