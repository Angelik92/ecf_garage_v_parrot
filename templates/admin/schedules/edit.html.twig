{% extends 'base_db.html.twig' %}

{% block title %}Modifier l'horaire{% endblock %}

{% block body %}
    <div class="container-fluid d-flex flex-column align-items-center justify-content-evenly">
        <h1 class="text-light fw-bold" >Modifier l'horaire</h1>

        {{ include('admin/schedules/_form.html.twig', {'button_label': 'Modifier'}) }}

        <a class="btn btn-primary" href="{{ path('schedules_index') }}"><i class="bi bi-arrow-return-left"></i> Retour à la liste</a>

    </div>
{% endblock %}
